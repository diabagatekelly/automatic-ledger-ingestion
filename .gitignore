# --- Secrets (never commit) ---
.env
.env.*
!.env.example
*.key
*.pem
service-account*.json
credentials*.json
gcp-*key*.json

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/
*.egg-info/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Claude local/session context (not published) ---
CLAUDE.local.md
docs/STATUS.md
docs/WORKFLOW.md

# --- Eval receipt photos: real customer receipts w/ PII — keep LOCAL only ---
# The public repo must not publish real business/client data. The eval runner
# (scripts/eval-gemini.py) skips any image case whose file is absent, so the
# text half stays reproducible for anyone; drop the photos here to run the
# image half locally (see docs/EVAL_BASELINE.md).
evals/images/
